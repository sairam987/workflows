name: Lint on Pull Request

on:
  pull_request:
    branches:
      - feature/**

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        id: checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        id: setup_node
        uses: actions/setup-node@v4
        if: steps.checkout.outcome == 'success'
        with:
          node-version: '18'

      - name: Set up Python
        id: setup_python
        uses: actions/setup-python@v4
        if: steps.setup_node.outcome == 'success'
        with:
          python-version: '3.11'

      # - name: Install JS dependencies (if package.json exists)
      #   id: install_js_deps
      #   if: steps.setup_python.outcome == 'success' && exists('package.json')
      #   run: |
      #     npm ci || npm install

      # - name: Install ESLint (if no package.json)
      #   id: install_eslint
      #   if: steps.install_js_deps.outcome == 'success' && !exists('package.json') && !hashFiles('**/node_modules/**')
      #   run: |
      #     npm init -y
      #     npm install --save-dev eslint

      # - name: Run ESLint (if JS files exist)
      #   id: run_eslint
      #   if: steps.install_eslint.outcome == 'success' && (contains(github.event.pull_request.changed_files, '') || steps.check_js.outputs.has_js == 'true')
      #   run: |
      #     npx eslint . || (echo 'ESLint found issues' && exit 1)
      #   env:
      #     CI: true

      # - name: Install flake8 for Python
      #   id: install_flake8
      #   if: steps.run_eslint.outcome == 'success' && (exists('requirements.txt') || exists('setup.py') || contains(github.event.pull_request.changed_files, ''))
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install flake8

      # - name: Run flake8 (if Python files exist)
      #   id: run_flake8
      #   if: steps.install_flake8.outcome == 'success'
      #   run: |
      #     flake8 . || (echo 'Flake8 found issues' && exit 1)

      # - name: Install markdownlint-cli
      #   id: install_md_lint
      #   if: steps.run_flake8.outcome == 'success'
      #   run: |
      #     npm install -g markdownlint-cli

      # - name: Run markdownlint
      #   id: run_md_lint
      #   if: steps.install_md_lint.outcome == 'success'
      #   run: |
      #     markdownlint '**/*.md' || (echo 'Markdownlint found issues' && exit 1)
